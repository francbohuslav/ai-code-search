# Path to the directory containing individual projects (subdirectories).
# E.g. on Windows: C:\Projekty\sources
# On Linux/macOS: /home/user/projects/sources
# This value can also be provided via environment variables from MCP clients / IDEs;
# the .env file is mainly for local development and running the HTTP server.
SOURCES_DIR=/path/to/sources

# Path to codebase-list.json file containing list of available codebases.
# E.g. on Windows: C:\Projekty\codebase-list.json
# On Linux/macOS: /home/user/codebase-list.json
# This value can also be provided via environment variables from MCP clients / IDEs.
CODEBASE_LIST_PATH=/path/to/codebase-list.json

# Port on which the web server will run (optional, default 8000).
PORT=8000

# Agent type: "cursor" | "gemini" | "claude" (optional, default "cursor").
# AGENT_TYPE=cursor

# Command to run cursor-agent or Gemini CLI (optional).
# For cursor: default "cursor-agent". For gemini: default "gemini".
# Not used when AGENT_TYPE=claude (Claude Agent SDK runs in-process).
# AGENT_CMD=cursor-agent

# Model for the agent (optional). Used by all backends when set.
# Cursor: --model value (default "auto"). Gemini: -m value. Claude: SDK model option.
# AGENT_MODEL=claude-sonnet-4-20250514

# When AGENT_TYPE=claude: set ANTHROPIC_API_KEY in your environment (required for Claude).
# The Claude Agent SDK reads it automatically.
